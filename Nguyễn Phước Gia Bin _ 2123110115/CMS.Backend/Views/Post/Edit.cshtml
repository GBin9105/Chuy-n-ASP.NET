@model CMS.Data.Entities.Post
@{
    ViewData["Title"] = "Sửa Bài Viết";
}

<div class="container-fluid mt-4">
    <div class="card shadow-sm border-0 col-md-10 mx-auto">
        <div class="card-header bg-white border-bottom pt-4 pb-3">
            <h4 class="font-weight-bold text-uppercase mb-0">Cập Nhật Bài Viết</h4>
        </div>
        <div class="card-body p-4">
            <form asp-action="Edit" method="post" enctype="multipart/form-data">
                <input type="hidden" asp-for="Id" />
                <input type="hidden" asp-for="ImageUrl" /> <div class="row">
                    <div class="col-md-8 form-group mb-3">
                        <label class="fw-bold">Tiêu đề bài viết <span class="text-danger">*</span></label>
                        <input asp-for="Title" class="form-control" required />
                    </div>

                    <div class="col-md-4 form-group mb-3">
                        <label class="fw-bold">Danh mục <span class="text-danger">*</span></label>
                        <select asp-for="CategoryId" class="form-select" asp-items="ViewBag.CategoryList" required>
                            <option value="">-- Chọn danh mục --</option>
                        </select>
                    </div>
                </div>

                <div class="form-group mb-3">
                    <label class="fw-bold d-block">Ảnh bìa hiện tại</label>
                    @if (!string.IsNullOrEmpty(Model.ImageUrl))
                    {
                        <img src="@Model.ImageUrl" alt="Current Cover" style="max-height: 150px;" class="mb-2 rounded border" />
                    }
                    <br />
                    <label class="fw-bold">Chọn ảnh mới (Bỏ trống nếu muốn giữ ảnh cũ)</label>
                    <input type="file" name="uploadImage" class="form-control" accept="image/*" />
                </div>

                <div class="form-group mb-4">
                    <label class="fw-bold">Nội dung chi tiết</label>
                    <textarea asp-for="Content" class="form-control" rows="8"></textarea>
                </div>

                <div class="d-flex justify-content-end">
                    <a asp-action="Index" class="btn btn-secondary me-3">Hủy bỏ</a>
                    <button type="submit" class="btn btn-warning px-4 fw-bold">
                        <i class="bi bi-pencil-square"></i> Cập Nhật
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>